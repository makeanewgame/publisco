# Notlar ve Yapılacaklar

Bu dosya, proje hakkında düşüncelerini, fikirlerini ve karşılaştığın sorunları kaydetmen için hazırlanmıştır.

**`[fable]` işareti**: başında bu ibare olan maddeler bilinçli olarak fable'a ayrılmıştır — denenip çıkmaza girilmiş, farklı bir bakış açısı gereken işler. İşaretli maddenin gövdesinde ELENEN yaklaşımlar da yazılıdır; aynı çıkmazlara tekrar girilmesin diye önce onlar okunmalı.

Tamamlanan maddeler burada kalabalık yapmasın diye [`TAMAMLANANLAR_v2.md`](./TAMAMLANANLAR_v2.md) dosyasına taşınıyor (eski arşiv: [`TAMAMLANANLAR.md`](./TAMAMLANANLAR.md), 2026-09-07'de dondu). Bir madde tamamlanırken ileride yapılması gereken bir iş ortaya çıkarsa, o iş ayrı bir madde olarak burada kalır.

## Yapılacaklar
- [ ] (2026-08-16) Dönüşüm kotası bayt yerine sayfa bazlı hale getirildi (free=500, premium=5000 sayfa, bkz. `apps/api/src/quota/quota.constants.ts`) — sayfa sayısı `convert.service.ts`'in `fetchPageCount`'unda Modal'ın `/analyze` endpoint'i (artık `page_count` de dönüyor, bkz. `converter.py` — `analyze_pdf`) çağrılarak her `/convert` isteğinde bağımsızca hesaplanıyor. Bu, PDF'in Modal tarafında bir kez daha (frontend zaten dosya seçilir seçilmez `/analyze`'ı bir kez tetikliyor, `run_pipeline`'ın plan fazı da kendi indirmesini yapıyor) indirilip açılması anlamına geliyor — küçük bir gecikme/egress maliyeti ama sayfa sayısını rezervasyondan önce güvenilir şekilde bilmenin yolu bu. **`modal deploy modal_worker/main.py` ile yeniden deploy edilmeden bu değişiklik prod'da etkili olmaz** (Modal ayrı deploy ediliyor, bkz. CLAUDE.md Quick Start).
- [ ] `pnpm audit`: 49 zafiyetten 46'sı kök `package.json`'a eklenen `pnpm.overrides` (tar, glob, picomatch, webpack, tmp, qs, body-parser, ajv, file-type, @xmldom/xmldom, fast-xml-parser, uuid, send, postcss, multer) ve `apps/web`'de vite'ın 5→6.4.3'e yükseltilmesiyle giderildi (api build, web build ve `expo` CLI ile doğrulandı). Kalan 2 kalem:
  - `@nestjs/core`'daki SSE injection açığı (GHSA-36xv-jgw5-4q75, moderate) sadece NestJS `11.1.18+`'ta düzeliyor — `@nestjs/common`/`core`/`config`/`jwt`/`passport`/`platform-express`'in hepsini major sürüm 10→11'e yükseltmek gerekiyor. Kod tabanında SSE (Server-Sent Events) kullanımı olmadığı için şu an sömürülemez; büyük/riskli bir migration olduğundan bilinçli olarak ertelendi.
  - `image-size` (high x2, `apps/mobile` → `react-native`/`metro` toolchain) için upstream'de henüz hiç yama yok ("patched version: yok") — sadece dev/build zamanında (Metro paketleyicisi) etkili, kabul edilmiş risk olarak takipte kalsın, yeni bir sürüm çıkınca `pnpm audit` tekrar kontrol edilmeli.
- [ ] SQL injection testleri yapılsın (bitince)
- [ ] Kota limitleri artık kullanıcı bazlı ayrı bir tabloda (`UserQuota`, `apps/api/prisma/schema.prisma`) tutuluyor ama bunu değiştirecek bir admin endpoint/UI yok — şu an sadece signup'ta tier'ın varsayılan değerleriyle dolduruluyor, kimseye özel limit tanımlanamıyor
- [ ] **GOLDEN VERİ KALİTESİ — bilinmesi gerekenler** (2026-08-20'de 47 kitabın `metadata.json`'ı dolduruldu; 2026-09-07 denetiminde teyit edildi). Değerler gerçek pipeline çalıştırılmadan, PDF'ten çıkarılan profillerle TAHMİN edildi — bu yüzden bazı metrikler gerçek kalite yerine golden hatasını ölçüyor olabilir:
  - `expected_paragraph_range` ham metin bloğu sayısından (~%20 tolerans) türetildi. Bunun en az bir kitapta YANLIŞ BİRİMDEN türetildiği kanıtlandı ve düzeltildi (`son-sans`, bkz. TAMAMLANANLAR_v2.md); 12 kitaplık belgesel denetimde hatanın izole olduğu doğrulandı. Yine de **`structure` metriğindeki ±0.5'lik oynamalar tek başına kalite sinyali sayılmamalı.** Çok sütunlu/görselli kitaplarda kalan aralıkları denetleyecek güvenilir bir yöntem yok (geometrik tahminci yalnızca tek sütunlu düz nesirde geçerli) — elle örnekleme gerekir.
  - `expected_image_count` gömülü görsel xref sayısından tahmini — EPUB'a çevrilince dedup/satır-içi mantığı yüzünden gerçek sayı farklı çıkabilir (bkz. aşağıdaki `book-with-tables_table` maddesi).
  - Tamamen taranmış 8 kitapta (`book-with-images_haritalarla-cografya`, `mathematical_test-soruolar`, `multilingual_azerice-kitap`, `poor-quality-scan_dikenler-sehri`, `scanned-novel_sevgili-tas-kalbim`, `scanned_001`, `scanned_003`, `turkish_carpik-ask`) title/author/must_include_phrases yalnızca birkaç örnek sayfanın görsel okunmasına dayanıyor — ileride yanlış bulunursa düzeltilmeli.
  - Yerel kurulum: `eval/requirements-eval.txt`'teki `wordfreq`/`pytest` ve (opsiyonel) `brew install epubcheck` venv'e kurulmalı (bkz. `modal_worker/eval/README.md`, `pnpm test:conversion`).
- [ ] **Hostinger VPS'teki eski worker deployment'ı durdurulup silinmeli** — PDF→EPUB dönüştürme 2026-08-11'de Hostinger'daki `apps/worker` (FastAPI)'dan Modal.com'a (`modal_worker/`) taşındı; repodaki `apps/worker` dizini artık git'te takip edilmiyor (yalnızca yerel bir kalıntı), `VITE_WORKER_URL`/`WORKER_URL` referansları kodda hiç kalmadı. Bu maddenin diğer adımları (ilk `modal deploy` + `publisco-secrets`, Vercel prod env'ine `MODAL_WEBHOOK_SECRET`/`MODAL_ENDPOINT_URL`, Vercel Blob HTTP API'sinin canlıya karşı doğrulanması) 2026-08 içinde tamamlandı — prod'da çalışan Modal deploy'u ve `api.publisco.com` üzerinden doğrulanan webhook route'u bunu teyit ediyor. Geriye yalnızca VPS temizliği kaldı.
- [ ] Lemon Squeezy entegrasyonu (`apps/api/src/payments`) kod tarafında hazır ama Lemon Squeezy dashboard'unda henüz store/ürün/varyant (5.99$/ay Premium plan) oluşturulmadı — `LEMONSQUEEZY_STORE_ID`/`LEMONSQUEEZY_PREMIUM_VARIANT_ID`/`LEMONSQUEEZY_API_KEY`/`LEMONSQUEEZY_WEBHOOK_SECRET` dolana kadar `/premium` sayfasındaki upgrade akışı çalışmaz
- [ ] Webhook (`POST /api/payments/webhook`) local'de test edilemiyor çünkü Lemon Squeezy `localhost`'a erişemiyor — ngrok ile tünel açılıp o URL webhook ayarlarına girilmeli, ya da dashboard'un "resend webhook" özelliği kullanılmalı
- [ ] `subscription_cancelled` webhook'u geldiğinde `membershipTier` bilinçli olarak değiştirilmiyor (kullanıcı dönem sonuna kadar premium kalmalı); ama gerçek `subscription_expired` webhook'u gelmezse (Lemon Squeezy tarafında bir sorun olursa) kullanıcı süresiz premium kalır — bunun için bir güvenlik ağı (ör. `endsAt` geçmiş ama hâlâ PREMIUM olan kullanıcıları periyodik kontrol eden bir job) yok
- [ ] Ödeme/webhook akışı için henüz otomatik test yazılmadı
- [ ] Koşu başlığı/gürültü filtrelerinin (`modal_worker/converter.py`) kalan tek bilinen zayıf noktası: `NOISE_MAX_CHARS=20`'ye kadar olan sayısal liste öğeleri ("14.", "1)") yanlışlıkla gürültü sayılıp silinebilir — liste ağırlıklı içerikte gözle doğrulanmadı. (2026-08-11'deki daha geniş "eşikler gerçek kitapla doğrulanmadı" endişesi artık geçersiz: `detect_header_footer_margins` 47 kitaplık golden sette defalarca uçtan uca koştu ve `HEADER_FOOTER_MAX_MARGIN_RATIO=0.15`'in ürettiği agresif bandın somut etkileri `book-with-images_966108` üzerinde ayrıntılı incelenip iki ayrı fix'e dönüştü — bkz. TAMAMLANANLAR.md.)
- [ ] TOC hiyerarşisi (başlıkları H1/H2 olarak sınıflandırma) ve sayfa-sınırı cümle birleştirme için LLM kullanımı konuşuldu ama hangi sağlayıcı/model (maliyet, gecikme, mevcut secret) kullanılacağına henüz karar verilmedi — karar verilince `modal_worker/main.py`'ye yeni bir secret (API key) ve `modal_worker/converter.py`'ye ilgili fonksiyonlar eklenecek.
- [ ] PUA (Private Use Area) glyph'leri Kindle'da kutu (□) olarak çıkıyor — `book-with-images_966108`'de φ/σ/τ gibi harfler `U+F066`/`U+F073`/`U+F074` olarak geliyor, EPUB kaynak fontu gömmediği için hem gövde metninde ("(□b)") hem formül satırlarında tofu görünüyor (2026-09-06 Kindle QA, bkz. `KINDLE_QA.md`).

  **(SYMBOL AİLESİ DÜZELTİLDİ, 2026-09-08 — bkz. TAMAMLANANLAR_v2.md)** 966108'deki PUA karakterlerinin tamamı `SymbolMT`'den geliyordu ve `0xF000 + ASCII` desenini izliyordu (Adobe Symbol kodlaması); statik tabloyla gerçek Unicode'a çevriliyor artık, o kitapta 281 kutu → σ/τ/φ oldu. Kapsam bilinçli olarak 0x20-0x7E ile sınırlı ve font adına gate'li.

  **AÇIK KALAN ÜÇ PARÇA:**
  1. **Wingdings/Webdings/FontAwesome PUA'sı** (külliyatta 2384 karakter, ağırlıklı `multilingual_azerice-kitap-2`) hâlâ kutu olarak çıkıyor. Bilinçli olarak dokunulmadı: oradaki glyph'ler harf değil DINGBAT (madde işareti, ok, kutu), Symbol tablosuyla çevrilseler anlamsız Yunan harfi olurlardı. Doğru çözüm ayrı bir Wingdings→Unicode tablosu (ör. `0xF06C` → `●`) olur ama her dingbat'ın makul bir metin karşılığı yok; bir kısmı için "sessizce sil" bile daha doğru olabilir. Değerlendirilmedi.
  2. **Symbol'ün ÜST ARALIĞI (0xA0-0xFE)** — büyük parantez/köşeli ayraç/integral MONTAJ PARÇALARI. Tek başına anlamlı Unicode karşılıkları yok (satır satır birleştirilmek üzere tasarlanmışlar). Külliyatta neredeyse tamamen `unsupported: true` matematik kitaplarında (`mathematical_franctional` 2778 karakter, `mathematical_order-integrations` 308) görüldüğünden kapsam dışı bırakıldı.
  3. **Unicode "Mathematical Alphanumeric Symbols" bloğu** (`𝜏𝜎𝑛𝑡𝑎𝑛𝑖`) — bunlar geçerli Unicode ama Kindle'ın varsayılan fontunda karşılığı yok, aynı tofu sonucunu veriyor. Çözüm normalizasyon (matematik-alfanümerik → düz Latin/Yunan) olabilir ama anlam kaybı riski var (italik/kalın matematik değişkeni ile düz harf ayrımı kaybolur), ayrıca değerlendirilmeli.

## Fikirler
- [ ] Çoklu dosya seçimi ve çoklu convert (bir seferde birden fazla PDF yükleyip sırayla dönüştürme) `ConvertPage.tsx`'ten kaldırıldı, şimdilik tek dosyalı akışa geçildi. İleride geri eklenecekse: `queuedFiles: File[]` state'i, dosya listesi UI'ı (`convert.selectedCount`), ve `handleConvert` içindeki dosya bazlı döngü + `progress.fileLabel` ({{index}}/{{count}}) tekrar kurulmalı — git geçmişinde referans olarak duruyor.
- [ ] Responsive tasarım olsun -- v1
- [ ] 50 MB'dan büyük dosyaları Kindle kabul etmiyor — bu limit arayüzde kontrol edilmeli -- lately v1
- [ ] `max_epub_size_mb` (hedef boyuta sıkıştırma, ör. Kindle 50MB) — eski `apps/worker/app/converter.py`'de vardı (görsel kalitesini kademeli düşürüp yeniden deneyen bir döngü), Modal'a taşırken (`modal_worker/converter.py`) bilinçli olarak v1 kapsamı dışında bırakıldı: map-reduce mimarisinde "hedefi tutturana kadar yeniden dene" tüm plan/map/reduce akışını (birden fazla görsel kalite profiliyle) tekrarlamayı gerektirir, bu da ilk sürümde gereksiz karmaşıklık. v2'de reduce fazına (`assemble_epub`) eklenebilir: EPUB üretildikten sonra boyut hedefi aşılıyorsa sadece görselleri (page_results'taki JPEG'leri) yeniden sıkıştırıp yeniden paketlemek, tüm map fazını tekrarlamaktan daha ucuz olur. (2026-09-06, Kindle QA sırasında alternatif öneri) Sıkıştırma yerine/yanında bölme: 50MB'ı aşan EPUB, bölüm sınırlarına göre "Kitap Adı - Bölüm 1", "Bölüm 2" ... şeklinde birden fazla EPUB'a bölünebilir — görsel kalitesinden ödün vermeden çözer, ama okuyucu deneyimini (tek kitap yerine N parça) değiştirir ve kullanıcıya N dosya sunmak/indirmek gerekir (UI/indirme akışı etkilenir). İki yaklaşım (sıkıştır vs. böl) birbirini dışlamaz, ikisi de v2 kapsamında değerlendirilebilir.
- [ ] eğlenceli temaya karar verince sitede 2 tema olsun biri eğlenceli diğeri ciddi switchin bir ucu fun diğer ucu ciddi olsun -- v2
- [ ] Kullanıcı font seçimi yapabilsin -- v2
- [ ] maille Kindle'a gönderebilsin -- v2
- [ ] Uygulama dil desteklerine ispanyolca da eklensin -- v2
- [ ] convert edilecek dokümanlara ön izleme özelliği ekleyelim -- v2

## Sorunlar (Buglar)
- [ ] (2026-09-06, boş-sayfa-atlama fix'i sonrası tam eval doğrulamasında bulundu) `technical-with-code_cplus-conherince` (84.5→81.3, -3.2), `scanned_002` (85.5→82.9, -2.6) ve `technical-with-code_functional-programing` (70.8→69.7, -1.1) skorları, gerçekten boş sayfaların artık eklenmemesiyle düştü (bkz. TAMAMLANANLAR.md, "Sayfa N" bulgusu). Flag'lenen sayfalar görsel olarak doğrulandı, hepsi tamamen boş, yanlış-pozitif yok — düşüş muhtemelen golden `expected_paragraph_range`/`expected_image_count` değerlerinin eskiden bu sayfaların ürettiği "Sayfa N" dolgu başlığını/görselini de sayarak kalibre edilmiş olmasından kaynaklanıyor, gerçek bir kalite kaybı değil. Bu üç kitabın golden `metadata.json`'ı gözden geçirilip beklenen değerler düzeltilmeli — 2026-09-07 itibariyle üçü de hâlâ ESKİ değerlerde (cplus 4664-6996, scanned_002 1150-1550, functional-programing 1608-2412), düzeltme notu içermiyorlar. `son-sans`'ta uygulanan yöntem (bkz. TAMAMLANANLAR_v2.md) burada da izlenebilir.
- [ ] (2026-09-07, dikenler-sehri fix'lerinin ARDINDAN kalan doğrulama — asıl bulgular kapandı, bkz. TAMAMLANANLAR.md'deki iki `poor-quality-scan_dikenler-sehri` maddesi) O kitaptaki iki kök neden de (gürültülü OCR x0'ı yüzünden cümle-ortası bölünme; OCR metninde sahte `<h2>`) düzeltildi ve fix'lerin İKİSİ DE kitaba özgü değil, tüm taranmış kitaplarda geçerli. Ama diğer düşük skorlu taranmış kitaplarda (`scanned_001` 49.8, `scanned_004` 53.1, `scanned-novel_sevgili-tas-kalbim` 38.6 — hepsi fix ÖNCESİ skorlar) kazancın ne olduğu ÖLÇÜLMEDİ. `--book` ile üçü tek tek koşturulup önce/sonra karşılaştırılmalı; kazanç varsa bu kitapların ayrıca araştırılmasına gerek kalmayabilir.
- [ ] (2026-09-06, Kindle QA'da bulundu, bkz. `KINDLE_QA.md`) `book-with-images_mitoloji-kitabi-alfa-yayinlari`'da ön-metin sayfaları (kapak, İçindekiler, Katkıda Bulunanlar) kötü çıkıyordu, ana mitoloji hikaye metni (tek sütun, 345 sayfanın büyük çoğunluğu) düzgündü. İKİ farklı kök neden vardı, biri kısmen düzeltildi:
  1. **(KISMEN DÜZELTİLDİ, 2026-09-06)** Çok-sütunlu ön-metin (İçindekiler 3 sütun, Katkıda Bulunanlar 2 sütun) satır satır karışıyordu (ör. "10 GiRiŞ 42 Zeus, hem ölümlü hem... ANTiKYUNAN Zeus'un türlü türlü ilişkileri" — 3 sütunun içeriği tek paragrafta harmanlanmış). Kök neden `page.get_text('dict')`'in kendi blok segmentasyonunun kısa-bloklu çok-sütunlu sayfalarda (TOC/katkıda-bulunanlar gibi) sütunları TEK bir blokta birleştirmesiydi (doğrulandı: sayfa 6'da 7 blok var, biri sayfa genişliğinin ~%88'ini kaplayıp üç sütunu birden içeriyordu) — `_detect_column_bands` bloklar üzerinde çalıştığından PyMuPDF'in birleştirdiği bir bloğu ayrıştıramıyordu. Fix: yeni `_unmerge_multi_column_block` (converter.py), `_collect_filtered_text_blocks`'ta her çok-satırlı bloğun KENDİ satırlarını `_detect_column_bands`'e vererek blok-içi gerçek sütun bandı olup olmadığını kontrol ediyor; bantlar bulunur VE birbiriyle ÇAKIŞMIYORSA (`_bands_are_disjoint` — çakışma, asılı/girintili tek-sütun paragraflarda gözlenen bir yanlış-pozitif işareti, geniş bir örneklemde 9 adaydan 8'i bu şekilde elendi) blok satır satır ayrıştırılıp sayfa-geneli okuma-sırası mantığına (`_split_into_reading_order_segments`) yeniden veriliyor. Test: `--book mitoloji-kitabi-alfa-yayinlari` ile skor 92.7 [Excellent]'e çıktı, TOC/katkıda-bulunanlar artık cümle-çorbası değil (her satır ayrı, okunabilir `<p>`) — ama KISMİ: sayfa-geneli sütun SIRASI (görsellerin sütunlar arasına interleave edilmesi yüzünden) hâlâ tam kusursuz değil, satırlar okunabilir ama column-major sırayla değil. Fast eval: 85.9→86.1 (+0.2), yeni regresyon yok, bonus `turkish_bilimsel-makale-nasil-yazilir` +1.9.
  **(2026-09-07, kök neden bulundu — fix ÖLÇÜLDÜ ama BİLİNÇLİ OLARAK ALINMADI)** Kalan sütun-sırası kusurunun kök nedeni tam olarak tespit edildi: `_detect_column_bands`, dokümante ettiği "bantlar çakışmaz" sözleşmesini ihlal edip ÇAKIŞAN bantlar dönebiliyor. Bandın sağ kenarı `max(x1)` olduğundan, bir sütunla aynı x0'dan başlayan TEK bir aşırı-geniş üye (sayfa 6'da "İÇİNDEKİLER" başlığı) bandın sağ kenarını komşu bandın x0'ına taşırıyor; o aralıktaki HER satır `_classify_block_band`'de "iki bandı birden kesiyor" (= tam-genişlik span) sayılıp kendi segmentine düşüyor. Sonuç: sayfa 6 → 75 segment, içindekiler maddeleri satır satır parçalanıyor.
  Denenen fix (bant sağ kenarını komşu bandın soluna kırpmak) sayfa 6'yı GERÇEKTEN düzeltiyor: 75 segment → 6, içindekiler 74 parça → 25 tutarlı madde (EPUB'da doğrudan doğrulandı, kitap skoru 92.7 / Structure %100). ANCAK ölçüldü ki aynı kırpma `turkish_bilimsel-makale-nasil-yazilir`'ın 196 sayfasından 7'sinde (sözlük/kısaltma listesi sayfaları — "Abstr./Acad.", "acetylglucosamine/acid-fast bacteria" gibi) ZARAR veriyor: orada ham/çakışan bantlar sayesinde her terim "span" sayılıp kendi paragrafı oluyordu (bir sözlük için DOĞRU davranış), kırpma bunu bozup terimleri birbirine yapıştırıyor — paragraf sayısı 2006→1532, kitap skoru 86.0→85.5 ve golden `expected_paragraph_range`'in altına düşüyor.
  Otomatik bir ayırt edici arandı, BULUNAMADI: aykırı-değer sayısı ayırmıyor (mitoloji 1/23, turkish sayfa 166 da 1/28), bant iç-içe geçmesi/kısmi çakışma da ayırmıyor (turkish sayfa 172 mitolojiyle aynı şekilde kısmen çakışıyor). Bu yüzden kırpma ALINMADI — bilinen bir kitabı düzeltmek için başka bir kitaba bilinçli regresyon sokmak kötü bir takas görüldü, üstelik kullanıcı iki varyantı Kindle'da karşılaştırdığında "ikisi de aynı derecede kötü" demişti (yani bu incelik kitabın genel izlenimini belirleyen asıl etken değil; asıl etken muhtemelen madde 2'deki kapak).

  **`[fable]` (2026-09-08, İKİNCİ TUR — ayırt edici BULUNDU, ama İKİNCİ bir hata modu ortaya çıktı; kod yazıldı, ölçüldü ve GERİ ALINDI)** Yukarıda "bulunamadı" denen sinyal bulundu: **doluluk profili**. Sarmalanmış metinde satırlar sütunu doldurur ve genişlikleri tekdüzedir (satır sonu sütun dolduğu için gelir); kendi başına tamam liste öğelerinde her öğe kendi doğal uzunluğunda biter, bu yüzden hem dar hem çok değişkendirler. Ölçüldü (bant başına, kırpılmış genişliğe göre): mitoloji İÇİNDEKİLER/KATKIDA BULUNANLAR sayfaları **doluluk 0.62-0.80 / değişim katsayısı 0.31-0.42**; turkish sözlük-kısaltma sayfaları **0.26-0.48 / 0.62-0.83**. İki metrik de temiz ayırıyor. Eşiklerle (`fill>=0.55, cv<=0.50`) uygulandığında hedef kitapta 43 çakışan sayfanın 40'ı kırpılıyor, **regresyon kitabında 7 sayfanın 0'ı** — yani yukarıdaki takas ortadan kalkıyor (`turkish_bilimsel` 86.0'da sabit kaldı, koşulsuz kırpma onu 85.5'e düşürüyordu). Mitoloji sayfa 6: 83 blok → 71 segment iken 5 segment. Fast eval 86.5'te değişmedi, suite 83/83 + 3 yeni test (kırpma kapalı ve KOŞULSUZ hallerinde ayrı ayrı kırmızı olduğu doğrulandı).
  **Yine de ALINMADI**, çünkü etki alanı ölçülünce (külliyatta 619 çakışan sayfanın 492'si kırpma kapsamına giriyor) YENİ ve GERÇEK bir hasar çıktı: `book-with-images_dunya-tarihi-alfa-yayinlari` 95.7→94.4, ve düşüşün tamamı **kırık kelime sayısının 230→481'e ikiye katlanmasından** geliyor (tekrar blokları 813→797, yani metrik yanılsaması DEĞİL; `text_completeness` %100'de kalıyor, içerik kaybı yok). Mekanizma: bant sağ kenarı komşunun soluna çekilince, o sütundaki MEŞRU geniş satırlar artık "iki bantla kesişiyor" sayılıp tam-genişlik span'e düşüyor ve paragraf cümle/kelime ortasından kopuyor. Denenen ikinci daraltma ("kırpma noktasını az sayıda üye aşıyorsa kırp") de ELENDİ: taşan üye oranı mitoloji'de 0.47, dunya-tarihi'nde 0.27 — yani hedefi eler, zararlıyı elemez.
  **Karar dayanağı:** hasar ÖLÇÜLÜ (kırık kelime ×2), kazanç ise ÖLÇÜLEMİYOR — hedef kitabın skoru 92.7'de hiç kıpırdamıyor (Structure zaten %100) ve eval'ın `Reading order` metriği bu kitapların HEPSİNDE `n/a`, yani düzeltmeye çalıştığımız şeyi ölçen bir metrik yok. Ölçülü zarar + ölçülemeyen fayda kombinasyonu sevk etmemeyi gerektirdi. **Bu maddeye dönecek olan için sırada ne var:** (a) doluluk-profili ayırt edicisi çalışıyor, tekrar keşfedilmesine gerek yok; (b) çözülmesi gereken YENİ problem, kırpma sonrası meşru geniş satırların span'e düşmesini engellemek (ör. bandı komşunun x0'ına değil, aykırı değer hariç üyelerin gerçek `max(x1)`'ine çekmek — denenmedi); (c) `Reading order` metriğini bu kitaplar için çalışır hale getirmeden bu iş kolunda ilerlemenin ölçüsü yok, belki önce o yapılmalı.
  2. **`[fable]` (AÇIK — 2026-09-07'de araştırıldı, ÜÇ YAKLAŞIM DA ELENDİ, aşağıdaki çıkmazları tekrarlamayın)** Kapak sayfası anlamsız metin üretiyor (ör. "SAllA ICAtJI ICAilliTIIl,llAI\," , "Ailtlf HfJJliı. ÖkÜYOk"). ÖNEMLİ DÜZELTME: bu bizim OCR'ımız değil — **PDF'in KENDİ gömülü metin katmanı bozuk** (sayfa 1'de 224 karakterlik çöp metin var; muhtemelen üreticinin kötü OCR'ı ya da bozuk font kodlaması). Biz bunu sadakatle çıkarıyoruz. Sayfa 2-3'te hiç metin olmadığı için onlar zaten görsele düşüyor; sayfa 1 "metni var" sayıldığından düşmüyor. Doğru davranış sayfayı görsel olarak gömmek olurdu ama "bu metin katmanı çöp mü" kararı güvenilir verilemedi:
     - **(a) Dilbilimsel "şüpheli token oranı"** (harf+rakam karışımı, kelime içi tuhaf noktalama, düzensiz büyük/küçük geçişi): kapak %32 çıkıyor ama `book-with-images_966108` sayfa 61 %12, kod ağırlıklı `cplus-conherince` %8 — marj yetersiz, üstelik kapakta yalnızca 28 token olduğundan oran gürültülü. DAHA KÖTÜSÜ: Türkçe'de noktalı/noktasız i yüzünden büyük harfli gerçek başlıklarda ("İÇİNDEKİLER") küçük→büyük geçişi doğal olarak oluşuyor, kural bunları ÇÖP sanıyor; buna karşılık asıl çöp satırları ("SAllA ICAtJI...", "Ailtlf HfJJliı. ÖkÜYOk") kaçırıyor. Yani Türkçe için temelden hatalı, ters yönde çalışıyor.
     - **(b) Yapısal "tam sayfa görsel"** sinyali: bu kitapta işe yaramıyor çünkü HER sayfada tam sayfa arka plan görseli var (sayfa 101'de %100 kaplama + 529 token sağlıklı metin).
     - **(c) "Tam sayfa görsel + az token"** bileşimi: kapağı (32 token) yakalar ama meşru seyrek sayfaları da (iç kapak, künye, tam sayfa levha + kısa altyazı) yakalar — yanlış karar GERÇEK METNİ SİLMEK demek, kabul edilemez risk.
     Sonuç: güvenilir çözüm sözlük/lexicon tabanlı bir doğrulama (ör. `wordfreq` ile gerçek kelime oranı) gerektiriyor; bu, `converter.py`'ye yeni bir bağımlılık + Modal imajına paket eklemek demek (bkz. CLAUDE.md "Sunucu Paketleri"), ayrı ve bilinçli bir karar olmalı. Sezgisel yaklaşımlarla denenip ürüne sokulmamalı.
  **Not:** Aynı "PyMuPDF blok segmentasyonu" kök nedeninin FARKLI bir alt-türü `mathematical_gumusfenbil`'deki "Abstract" başlığı sorunu — o TEK sütunlu bir sayfada bitişik öğelerin (metadata satırı + başlık + paragraf) birleşmesi, yukarıdaki fix'in hedeflediği ÇOK-SÜTUNLU birleşmeden farklı bir geometri (`_detect_column_bands` tek sütunlu içerikte hiç bant bulamıyor, dolayısıyla tetiklenmiyor) — doğrulandı, hâlâ düzeltilmedi (bkz. `mathematical_gumusfenbil` maddesi).
  **(2026-09-07 araştırması — çözüm YOLU BULUNDU ama BİLİNÇLİ OLARAK ERTELENDİ)** Sinyal aslında çok temiz: o blokta ("Abstract" bloğu, 12 satır, y=197.7-314.5) `_line_text_and_font` ile bakıldığında **"Abstract" satırı blok içinde tek başına `kalın=True`**, altındaki gövde satırları aynı puntoda (10.1) ama kalın değil, üstündeki metadata satırları ise daha küçük (7.9). Yani "çok satırlı bloğu, çevresi kalın-olmayan tek bir KALIN + başlık-şekilli satırda böl" kuralı bu vakayı doğrudan çözer. ERTELEME GEREKÇESİ: kural tüm külliyatta ölçüldü ve çok satırlı 61.683 bloğun **%2.4'ünde (1485 blok)** tetikleniyor — `dunya-tarihi` 552, `azerice-kitap-2` 297, `bilim-kitabi` 182 gibi doğrulaması zor kitaplarda yoğunlaşıyor. Buna karşılık hedef kitap golden'da zaten `unsupported: true` (matematik kategorisi, v1 kapsamı dışı). Yani doğrulayamayacağım onlarca kitapta blok bölme riski, kapsam dışı tek bir kitabı düzeltmek için alınamaz. Not: "kalın dizinin ORTASINDA bölme" (iki satıra taşmış kalın başlıkları yarmamak için `info[i-1]` kalınsa atla) daraltması denendi, 1573→1485'e indiriyor — ileride bu iş ele alınırsa bu daraltma zaten gerekli. Bu maddeye dönülürse: sinyal hazır, eksik olan tek şey geniş kapsamlı doğrulama (etkilenen kitaplarda önce/sonra EPUB karşılaştırması).
- [ ] **`mathematical` kategorisi v1'de KAPSAM DIŞI — bu bilinçli kararın somut teyitleri** (2026-09-06 Kindle QA, bkz. `KINDLE_QA.md`). Golden'daki 9 matematik kitabının hepsi `unsupported: true`; aşağıdakiler yeni bug değil, kapsam kararının gerekçesi. v2'de bu kategoriye dönülürse başlangıç noktası burası:
  - `mathematical_test-soruolar` (440 sayfa, tamamen taranmış): OCR'ın ürettiği anlamsız metin gerçek içeriğin yerini alıyordu. `try_ocr_page`'e eklenen `min_confidence` eşiği görsel sayısını 13→76'ya çıkardı ama YETERSİZ — Tesseract matematik notasyonunu görece yüksek bir "güvenle" (%72.9 ortalama) ama semantik olarak YANLIŞ okuyor; confidence eşiği bu "sistematik-yanlış-ama-emin" okumaları yakalayamıyor. (Fix'in asıl değeri genel olarak düşük-güvenli taranmış sayfalarda, bu kitaba özel değil — bkz. TAMAMLANANLAR.md.)
  - `mathematical_order-integrations` (taranmamış, gömülü metin katmanı VAR): kesir/limit/üst-alt simge içeren tanımlar dağılmış parçalar olarak çıkıyor ("x f h x f d", "lim ) ("). FARKLI mekanizma — burada OCR yok; PDF'in matematik dizgisi ayrı ayrı konumlandırılmış küçük metin parçalarından oluşuyor ve okuma-sırası birleştirme mantığımız bunları anlamlı sıraya koyamıyor.
  - Her ikisi için de gerçek çözüm metin-tabanlı yaklaşımın ötesinde olurdu (ör. formül yoğunluğu yüksek bölgeleri sezip görsel/kırpma olarak gömmek).
- [ ] (2026-08-27, `detect_recurring_margin_text` fix'i sırasında bulundu — bkz. TAMAMLANANLAR.md, `two-column-academic_elektrofarazi` maddesi) `two-column-academic_elektrofarazi`'de sayfa 1'e özgü, TEK SEFER görünen 108 karakterlik bir masthead bloğu (`"Dicle Tıp Dergisi / ... doi: ..."`, golden'ın `"Dicle Medical Journal"` exclude-phrase'ini içeriyor) hâlâ sızıyor — hem `HEADER_FOOTER_MAX_CHARS` (60) hem `BLACKLIST_SUBSTRING_MAX_CHARS` (90) kapağını aşıyor, tekrarlamadığı için yeni içerik-tabanlı `detect_recurring_margin_text` de yakalayamıyor. Kabul edilen küçük bir sınırlama (kitap skoru zaten 87.7, [Good]); cap'i tekrar yükseltmek `book-with-images_966108` regresyon riskini geri getirir. Benzer tek-seferlik uzun masthead/kapak-metni sızıntıları başka bir kitapta gözle görülür şekilde tekrar ederse buraya dönülüp bakılmalı.
- [ ] (2026-08-27/28, aynı fix'in tam 50 kitaplık eval doğrulamasında bulundu) `multilingual_azerice-kitap-2` skoru 77.0→73.4 (-3.6) geriledi — `text_completeness` (%0→%100) ve `structure` (%38.8→%69.5) belirgin şekilde İYİLEŞTİ (muhtemelen sayfa başına tekrarlayan `"bilikliol.az"` filigranının artık `detect_recurring_margin_text` ile doğru süzülmesi sayesinde) ama `images` (%93.7→%42.6) ve `ocr_quality` (%79.5→%59.0) düştü — bu ikisinin fix'le nedensel bağlantısı araştırılmadı (kitap zaten çok bileşenli sorunları olan bir "Fair" tier OCR kitabı). Net -3.6 küçük kabul edilip bu turda kapsam dışı bırakıldı; images/ocr_quality düşüşünün gerçek bir yan-etki mi yoksa OCR/rastgelelik kaynaklı bir sapma mı olduğu bir sonraki turda `--book multilingual_azerice-kitap-2` ile teşhis edilmeli.

- [ ] Blob upload'daki 400'ü düzelttikten sonra (bkz. TAMAMLANANLAR_v2.md, `x-vercel-blob-access` header'ı eksikti) aynı job'da `run_pipeline`'ın sonunda attığı Vercel webhook'u (`post_vercel_webhook`) `404 {"detail":"Not Found"}` dönmeye başladı — bu gövde Nest'in değil FastAPI/Starlette'in varsayılan 404 formatı, yani `publisco-secrets`'taki `VERCEL_WEBHOOK_URL` gerçek Nest API'sine değil bir FastAPI uç noktasına (büyük ihtimalle Modal worker'ın kendi `MODAL_ENDPOINT_URL`'ine) işaret ediyordu. Bu aynı zamanda Modal'ın kendi loglarındaki tekrarlayan `POST / -> 404`'ü de açıklıyor (webhook kendi kendine POST atıyor). `curl -X POST https://api.publisco.com/api/webhooks/modal-result` ile doğru URL doğrulandı (401 "Invalid webhook signature" döndü — Nest doğru route'ta canlı). Kod tarafında düzeltilecek bir şey yok; `publisco-secrets`'taki `VERCEL_WEBHOOK_URL`'in Modal dashboard'undan `https://api.publisco.com/api/webhooks/modal-result` olarak güncellenmesi gerekiyor (kullanıcıya iletildi, henüz teyit bekleniyor).

- [ ] (2026-08-26, chapter-detection fallback'i eklenirken elle doğrulamada bulundu — bkz. ROADMAP.md/TAMAMLANANLAR.md madde 3) Bölüm tespiti fallback'i 5 golden kitabın hepsinde iyi ama MÜKEMMEL değil: `turkish_bilimsel-makale-nasil-yazilir`'de beklenen 2 bölümden yalnızca 1'i bulunuyor (kitapta ikinci başlığı gerçek başlıktan ayıracak güçlü bir font-boyutu/kalınlık sinyali yok gibi görünüyor — araştırılmadı, düşük öncelikli); `book-with-images_ankaranin-trekking-rotalari` ve `technical-with-code_functional-programing`'de gerçek bölümlerin hepsi bulunuyor ama birkaç makul-ama-fazladan aday da geliyor (sırasıyla bir alt-bölüm başlığı ve iki gerçek ek/appendix — zararsız, ama tam hassasiyet değil). Bunlar kabul edilen sınırlamalar (heuristic bir fallback'ten mükemmel semantik ayrım beklenmiyor), ama biri gerçek bir kitapta gözle görülür şekilde bozuk bölüm listesi üretirse buraya dönülüp tekrar bakılmalı.
- [ ] `scanned_002`'nin `input.pdf`'inde (`is_scanned=true` olmasına rağmen) gömülü bir metin/OCR katmanı zaten var ve bazı yerlerde bozuk (`"Eğer"` → `"Ecer"`) — bu tarayıcı yazılımının kendi OCR'ı, `modal_worker` pipeline'ının ürettiği bir şey değil. İleride bu kitapta "OCR kalitesi düşük" şikayeti gelirse, kaynağın bizim pipeline'ımız sanılmaması için akılda tutulmalı. (Aksiyon gerektirmeyen, teşhisi yanlış yöne saptırmasın diye tutulan bir not.)
- [ ] (2026-08-25, ROADMAP.md madde 2'nin görsel boyut-filtresi düzeltmesi sırasında bulundu; 2026-08-26'da `images` metriğinin benzersiz-dosya fix'iyle sayı güncellendi) `book-with-tables_table` golden kitabında yeni `MIN_EMBEDDED_IMAGE_DISPLAY_PT=30` eşiği bir mimari diyagramın küçük alt-ikonlarından bazılarını (ör. xref 254: 32.9x18.4pt "atlandı" vs xref 256: aynı diyagramda 32.9x31.2pt "tutuldu" — ikisi de görünüşe göre aynı şeklin parçası) tutarsız şekilde filtreliyor olabilir; golden `expected_image_count=44` zaten çok kaba bir tahmin (gerçek PDF'te 92 benzersiz-rect xref var, çoğu her sayfada tekrarlayan küçük şablon/logo ikonu) — hem golden veri hem eşik değeri belirsiz olduğundan bu turda araştırılmadı. `images` metriğinin artık benzersiz dosya sayısını (occurrence yerine) ölçmesiyle bu kitabın gerçek boşluğu daha görünür oldu: EPUB'da 17 benzersiz görsel dosyası var (34 `<img>` occurrence, çoğu sayfa başına tekrarlayan aynı küçük ikon), golden'ın 44'üyle aradaki fark büyük — kitap skoru 72.6→69.2. Daha fazla gerçek kitapla hem eşik (`MIN_EMBEDDED_IMAGE_DISPLAY_PT`) hem golden `expected_image_count=44` tahmini kalibre edilmeli.

## Çözüm Önerileri
- [ ] Convert'teki İptal butonu artık istemciyi sonucu beklemekten vazgeçiriyor ama Modal'daki `run_pipeline`'ı (bkz. `modal_worker/main.py`) sunucu tarafında durdurmuyor — pipeline kullanıcı vazgeçtikten sonra da container/OCR kaynağını tüketmeye (ve dolayısıyla Modal faturasını) etkilemeye devam ediyor. Gerçek bir sunucu-taraflı iptal için ya Modal'ın job iptal API'si (`modal.FunctionCall.cancel`) kullanılmalı ya da `ConvertJob`'a bir "cancelled" bayrağı eklenip `run_pipeline`'ın periyodik olarak (her chunk sonrası) bunu kontrol etmesi gerekiyor.
