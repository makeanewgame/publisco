# Notlar ve Yapılacaklar

Bu dosya, proje hakkında düşüncelerini, fikirlerini ve karşılaştığın sorunları kaydetmen için hazırlanmıştır.

Tamamlanan maddeler burada kalabalık yapmasın diye [`TAMAMLANANLAR.md`](./TAMAMLANANLAR.md) dosyasına taşınıyor. Bir madde tamamlanırken ileride yapılması gereken bir iş ortaya çıkarsa, o iş ayrı bir madde olarak burada kalır.

## Yapılacaklar
- [ ] (2026-08-16) Dönüşüm kotası bayt yerine sayfa bazlı hale getirildi (free=500, premium=5000 sayfa, bkz. `apps/api/src/quota/quota.constants.ts`) — sayfa sayısı `convert.service.ts`'in `fetchPageCount`'unda Modal'ın `/analyze` endpoint'i (artık `page_count` de dönüyor, bkz. `converter.py` — `analyze_pdf`) çağrılarak her `/convert` isteğinde bağımsızca hesaplanıyor. Bu, PDF'in Modal tarafında bir kez daha (frontend zaten dosya seçilir seçilmez `/analyze`'ı bir kez tetikliyor, `run_pipeline`'ın plan fazı da kendi indirmesini yapıyor) indirilip açılması anlamına geliyor — küçük bir gecikme/egress maliyeti ama sayfa sayısını rezervasyondan önce güvenilir şekilde bilmenin yolu bu. **`modal deploy modal_worker/main.py` ile yeniden deploy edilmeden bu değişiklik prod'da etkili olmaz** (Modal ayrı deploy ediliyor, bkz. CLAUDE.md Quick Start).
- [ ] `pnpm audit`: 49 zafiyetten 46'sı kök `package.json`'a eklenen `pnpm.overrides` (tar, glob, picomatch, webpack, tmp, qs, body-parser, ajv, file-type, @xmldom/xmldom, fast-xml-parser, uuid, send, postcss, multer) ve `apps/web`'de vite'ın 5→6.4.3'e yükseltilmesiyle giderildi (api build, web build ve `expo` CLI ile doğrulandı). Kalan 2 kalem:
  - `@nestjs/core`'daki SSE injection açığı (GHSA-36xv-jgw5-4q75, moderate) sadece NestJS `11.1.18+`'ta düzeliyor — `@nestjs/common`/`core`/`config`/`jwt`/`passport`/`platform-express`'in hepsini major sürüm 10→11'e yükseltmek gerekiyor. Kod tabanında SSE (Server-Sent Events) kullanımı olmadığı için şu an sömürülemez; büyük/riskli bir migration olduğundan bilinçli olarak ertelendi.
  - `image-size` (high x2, `apps/mobile` → `react-native`/`metro` toolchain) için upstream'de henüz hiç yama yok ("patched version: yok") — sadece dev/build zamanında (Metro paketleyicisi) etkili, kabul edilmiş risk olarak takipte kalsın, yeni bir sürüm çıkınca `pnpm audit` tekrar kontrol edilmeli.
- [ ] SQL injection testleri yapılsın (bitince)
- [ ] Şifremi unuttum akışında artık hesabın kayıtlı olup olmadığı sızdırılıyor (enumeration riski) — `AuthService.forgotPassword`'ün kayıtsız e-posta için 404 dönmesi bilinçli bir tercih olarak eklendi, ama halka açık/production bir ortama çıkmadan önce bu davranış tekrar değerlendirilmeli (rate limiting, captcha vb. önlemler düşünülebilir)
- [ ] Kota limitleri artık kullanıcı bazlı ayrı bir tabloda (`UserQuota`, `apps/api/prisma/schema.prisma`) tutuluyor ama bunu değiştirecek bir admin endpoint/UI yok — şu an sadece signup'ta tier'ın varsayılan değerleriyle dolduruluyor, kimseye özel limit tanımlanamıyor
- [ ] (2026-08-13) `modal_worker/eval/` altında conversion quality evaluation framework kuruldu (golden dataset + metrikler + skorlama + regresyon testi, bkz. `modal_worker/eval/README.md`, `pnpm test:conversion`) — pipeline koduna dokunmadı. `eval/requirements-eval.txt`'teki `wordfreq`/`pytest` ve (opsiyonel) `brew install epubcheck` yerel venv'e kurulmalı.
- [ ] (2026-08-20) Golden dataset'teki 47 gerçek kitabın hepsinde `metadata.json` dolduruldu (title/author/language/is_scanned/must_include-exclude_phrases/expected_chapters/expected_image_count/expected_paragraph_range/notes — `mathematical` kategorisindeki 9 kitap `unsupported: true` olarak işaretlendi, README'deki tier tanımına uygun). Doldurma, PDF'lerden çıkarılan sayfa/görsel/örnek-metin profillerine (pymupdf+langdetect, script commit edilmedi) ve tamamen taranmış (metin katmanı olmayan) 8 kitapta ek olarak birkaç sayfanın görsel olarak okunmasına dayandı. Bilinmesi gerekenler:
  - `expected_paragraph_range` değerleri gerçek pipeline (`converter.py`) çalıştırılmadan, yalnızca PDF'teki ham metin bloğu sayısından (~%20 tolerans) tahmin edildi — pipeline'ın kendi paragraf birleştirme/temizleme mantığı farklı bir sayı üretebilir. İlk `pnpm test:conversion` koşusundan sonra sapma büyükse (özellikle görsel-ağırlıklı kitaplarda, ör. `book-with-images_*` serisinde 3000-30000 aralığında tahminler var) bu aralıklar gözlenen gerçek sayıya göre daraltılmalı.
  - `expected_image_count` PDF'teki gömülü görsel xref sayısından tahmini — EPUB'a çevrilince dedup/satır-içi mantığı yüzünden gerçek sayı farklı çıkabilir.
  - Tamamen taranmış 8 kitapta (`book-with-images_haritalarla-cografya`, `mathematical_test-soruolar`, `multilingual_azerice-kitap`, `poor-quality-scan_dikenler-sehri`, `scanned-novel_sevgili-tas-kalbim`, `scanned_001`, `scanned_003`, `turkish_carpik-ask`) title/author/must_include_phrases birkaç örnek sayfanın görsel okunmasına dayanıyor — tüm kitap taranmadığından ileride yanlış bulunursa düzeltilmeli.
  - Sıradaki adım: gerçek bir `baselines/baseline.json` üretmek için `pnpm test:conversion --save-baseline` çalıştırmak (şu an yalnızca smoke kitaplarla kaydedilmiş durumda) ve yukarıdaki tahmini `expected_paragraph_range` değerlerini gözlenen sonuçlara göre kalibre etmek.
- [ ] PDF→EPUB dönüştürme Hostinger'daki `apps/worker` (FastAPI)'dan Modal.com'a (`modal_worker/`) taşındı (2026-08-11) — bu, eski `VITE_WORKER_URL`/`WORKER_URL` sorununu (bkz. eski not, artık geçersiz) kökten çözüyor çünkü ikisi de tamamen kaldırıldı. Kalanlar (repo dışı altyapı adımları, koda etki etmiyor):
  - `modal deploy modal_worker/main.py` ile ilk deploy yapılıp `modal secret create publisco-secrets BLOB_READ_WRITE_TOKEN=... MODAL_WEBHOOK_SECRET=... VERCEL_WEBHOOK_URL=...` ile secret oluşturulmalı
  - Vercel'deki `apps/api` prod env'ine `MODAL_WEBHOOK_SECRET`/`MODAL_ENDPOINT_URL` eklenip yeniden deploy edilmeli (Vite/Nest env'leri build-time'da gömülüyor, bkz. eski notun aynı dersi)
  - Hostinger VPS'teki eski worker deployment'ı durdurulup silinmeli
  - `modal_worker/main.py`'deki Vercel Blob HTTP API çağrıları (`upload_epub_to_blob`/`delete_blob`) canlı Vercel Blob dokümantasyonuna karşı doğrulanmadı (bu görev sırasında ağ erişimi bir tool tarafından engellendi) — ilk gerçek deploy'da mutlaka test edilmeli
- [ ] Lemon Squeezy entegrasyonu (`apps/api/src/payments`) kod tarafında hazır ama Lemon Squeezy dashboard'unda henüz store/ürün/varyant (5.99$/ay Premium plan) oluşturulmadı — `LEMONSQUEEZY_STORE_ID`/`LEMONSQUEEZY_PREMIUM_VARIANT_ID`/`LEMONSQUEEZY_API_KEY`/`LEMONSQUEEZY_WEBHOOK_SECRET` dolana kadar `/premium` sayfasındaki upgrade akışı çalışmaz
- [ ] Webhook (`POST /api/payments/webhook`) local'de test edilemiyor çünkü Lemon Squeezy `localhost`'a erişemiyor — ngrok ile tünel açılıp o URL webhook ayarlarına girilmeli, ya da dashboard'un "resend webhook" özelliği kullanılmalı
- [ ] `subscription_cancelled` webhook'u geldiğinde `membershipTier` bilinçli olarak değiştirilmiyor (kullanıcı dönem sonuna kadar premium kalmalı); ama gerçek `subscription_expired` webhook'u gelmezse (Lemon Squeezy tarafında bir sorun olursa) kullanıcı süresiz premium kalır — bunun için bir güvenlik ağı (ör. `endsAt` geçmiş ama hâlâ PREMIUM olan kullanıcıları periyodik kontrol eden bir job) yok
- [ ] Ödeme/webhook akışı için henüz otomatik test yazılmadı
- [ ] `modal_worker/converter.py`'deki yeni koşu başlığı/gürültü filtreleri (aşağıdaki "Sorunlar" bölümüne bkz.) heuristic — sayısal liste öğeleri ("14.", "1)") veya çok kısa tek-karakterlik başlıklar (nadir) yanlışlıkla silinebilir; gerçek kitaplarla (özellikle akademik/liste ağırlıklı içerik) test edilip eşikler (`HEADER_FOOTER_MAX_MARGIN_RATIO`, `NOISE_MAX_CHARS`) ayarlanmalı.
- [ ] Kenar payı kalibrasyonu (`detect_header_footer_margins`, `modal_worker/converter.py`) 20+ sayfalı kitaplarda kitaba yayılmış 10 sayfa örnekleyip gerçek header/footer bandını tahmin ediyor (2026-08-11) — henüz gerçek (sentetik olmayan) bir kitapla uçtan uca doğrulanmadı, ilk prod dönüşümlerinde `modal app logs`'taki "Kenar payı kalibrasyonu: ..." satırları kontrol edilip beklenen oranı buluyor mu teyit edilmeli.
- [ ] TOC hiyerarşisi (başlıkları H1/H2 olarak sınıflandırma) ve sayfa-sınırı cümle birleştirme için LLM kullanımı konuşuldu ama hangi sağlayıcı/model (maliyet, gecikme, mevcut secret) kullanılacağına henüz karar verilmedi — karar verilince `modal_worker/main.py`'ye yeni bir secret (API key) ve `modal_worker/converter.py`'ye ilgili fonksiyonlar eklenecek.
- [ ] "7 Messanda" / "a SE ." gibi anlamsız parçalanmış metinler muhtemelen özel/subset font'ların Private Use Area'ya remap edilmiş glyph'lerinden kaynaklanıyor (PDF'e özgü bilinen bir sorun) — regex tabanlı gürültü filtresiyle güvenilir şekilde çözülemez, düzeltmek için font encoding/cmap seviyesinde inceleme gerekir; şimdilik kapsam dışı.

## Fikirler
- [ ] Çoklu dosya seçimi ve çoklu convert (bir seferde birden fazla PDF yükleyip sırayla dönüştürme) `ConvertPage.tsx`'ten kaldırıldı, şimdilik tek dosyalı akışa geçildi. İleride geri eklenecekse: `queuedFiles: File[]` state'i, dosya listesi UI'ı (`convert.selectedCount`), ve `handleConvert` içindeki dosya bazlı döngü + `progress.fileLabel` ({{index}}/{{count}}) tekrar kurulmalı — git geçmişinde referans olarak duruyor.
- [ ] Responsive tasarım olsun -- v1
- [ ] 50 MB'dan büyük dosyaları Kindle kabul etmiyor — bu limit arayüzde kontrol edilmeli -- lately v1
- [ ] `max_epub_size_mb` (hedef boyuta sıkıştırma, ör. Kindle 50MB) — eski `apps/worker/app/converter.py`'de vardı (görsel kalitesini kademeli düşürüp yeniden deneyen bir döngü), Modal'a taşırken (`modal_worker/converter.py`) bilinçli olarak v1 kapsamı dışında bırakıldı: map-reduce mimarisinde "hedefi tutturana kadar yeniden dene" tüm plan/map/reduce akışını (birden fazla görsel kalite profiliyle) tekrarlamayı gerektirir, bu da ilk sürümde gereksiz karmaşıklık. v2'de reduce fazına (`assemble_epub`) eklenebilir: EPUB üretildikten sonra boyut hedefi aşılıyorsa sadece görselleri (page_results'taki JPEG'leri) yeniden sıkıştırıp yeniden paketlemek, tüm map fazını tekrarlamaktan daha ucuz olur.
- [ ] eğlenceli temaya karar verince sitede 2 tema olsun biri eğlenceli diğeri ciddi switchin bir ucu fun diğer ucu ciddi olsun -- v2
- [ ] Kullanıcı font seçimi yapabilsin -- v2
- [ ] maille Kindle'a gönderebilsin -- v2
- [ ] Uygulama dil desteklerine ispanyolca da eklensin -- v2
- [ ] convert edilecek dokümanlara ön izleme özelliği ekleyelim -- v2

## Sorunlar (Buglar)
- [ ] (2026-08-21, Faz 2 görsel çıkarımı sırasında bulundu) `extract_embedded_page_images` (`modal_worker/converter.py`) iki bilinçli sınırlamayla MVP olarak bırakıldı: (1) **sayfa-aşırı dedup yok** — aynı gömülü görsel (xref) birden fazla sayfada tekrar ediyorsa (ör. her sayfada aynı yayınevi logosu/filigran), dosya adı sayfa numarasını içerdiğinden her tekrarda ayrı bir dosya olarak ekleniyor, EPUB boyutunu gereksiz şişirebilir — sayfa-içi dedup (`seen_xrefs`) var ama sayfalar arası yok. (2) **Konumlandırma yaklaşık** — çıkarılan görseller o sayfanın metninin SONUNA ekleniyor, PDF'teki gerçek konumlarına (paragraf arasına) değil; tam interleaving için blok-seviyesi y-koordinat sıralaması (`_extract_text_blocks`'un zaten yaptığına benzer) gerekir, kapsam dışı bırakıldı. (3) Boyut filtresi (`MIN_EMBEDDED_IMAGE_DIMENSION=40px`) görselin PDF'teki ham piksel boyutuna bakıyor, sayfadaki GÖRÜNEN (ölçeklenmiş) boyutuna değil — küçük gösterilen büyük bir görsel ya da tersi yanlış filtrelenebilir. Golden set'te (2026-08-21) `images` metriği 0.492→0.828, gömülü görsel oranı %2.3→%68.6'ya çıktı (bkz. TAMAMLANANLAR.md) — kalan ~%31 kayıp büyük ölçüde bu üç sınırlamadan kaynaklanıyor olabilir, doğrulanmadı.
- [ ] (2026-08-22, golden veri denetimi sırasında bulundu, unsupported=true olduğu için ertelendi) `mathematical_test-soruolar` ve `mathematical_ujma`'nın `metadata.json`'ında aynı ifade (sırasıyla `"Scanned by CamScanner"` ve `"Universal Journal of Mathematics and Applications"`) hem `must_include_phrases` hem `must_exclude_phrases`'te birden duruyor — ikisi de PDF'in NEREDEYSE HER sayfasında tekrarlıyor (watermark / dergi başlığı), yani h1-hariç-tutma mekanizmasıyla (bkz. `[[text-completeness-exclude-h1]]`/TAMAMLANANLAR) çözülemeyecek gerçek bir çelişki: `evaluate_text_completeness` aynı anda "metinde var olmalı" ve "metinde olmamalı" isteyemez, skor her koşulda cezalanır. Her iki kitap da `unsupported: true` (mathematical kategorisi geneli) olduğundan şu an gerçek skor gate'ine girmiyor gibi görünüyor, bu yüzertelendi — ama düzeltilirse basitçe `must_exclude_phrases`'ten çıkarılmalı (aynı bu turda 10 kitapta yapılan düzeltmeyle aynı desen, bkz. TAMAMLANANLAR).
- [ ] (2026-08-22, golden veri denetimi sırasında bulundu, `mathematical_singular-integrals` çalıştırılırken gözlemlendi) PyMuPDF bu PDF'i açarken `MuPDF error: format error: No default Layer config` uyarısı basıyor (PDF'in Optional Content Group / layer yapılandırmasıyla ilgili) — kitap `unsupported: true` olduğundan şu an aksiyon alınmadı, ama pipeline'da sessizce yutuluyor olabilir; ileride `mathematical` desteği ele alınırsa bu PDF'in katman yapısı ayrıca incelenmeli.
- [ ] (2026-08-23, Faz 4 sonrası fast-eval'de hâlâ düşük çıkınca bulundu) `book-with-images_966108` (149 sayfalık jeoloji tezi) skoru 53.4 — `structure` metriği hâlâ çok düşük (%22.9): pipeline yalnızca 699 paragraf üretiyor, golden metadata'daki `expected_paragraph_range` [3051, 4576] bekliyor. Kök neden DOĞRULANMADI, iki rakip hipotez var: (a) gerçek bir metin/paragraf kaybı bug'ı (belki tablo-ağırlıklı sayfalarda — kitapta çok sayıda tekrarlayan tablo verisi var, bkz. TAMAMLANANLAR'daki `duplicate_ngram_ratio` notu), (b) golden verideki tahmin zaten yanlış — `expected_paragraph_range` gerçek pipeline çalıştırılmadan, ham PDF blok sayısından %20 toleransla tahmin edilmişti (bkz. 2026-08-20 tarihli genel not, aşağıda) ve tablo-ağırlıklı belgelerde ham blok sayısı gerçek "paragraf" sayısından çok daha yüksek çıkabilir. İlk 30 sayfada raw block'ların yalnızca %11'i çok satırlı olduğundan Faz 3'ün çok-satırlı-blok-hep-tek-paragraf kuralı tek başına açıklayıcı görünmüyor. İncelenmeden karar verilmemeli.
- [ ] (2026-08-23, Faz 4 sonrası fast-eval'de hâlâ düşük çıkınca bulundu) `complex-headings_tu-rkiye-sigorta-klavuz` skoru 58.8, 2 must_include_phrase hâlâ kayıp: (1) `"Müşteri Platformu Sağlık Kullanıcı Kılavuzu"` — kapak sayfasının ham PDF metninde `"S ağ lık K u llan ıc ı Kılavu zu"` gibi harfler arasına sahte boşluklar girmiş halde geliyor (muhtemelen kapak tasarımındaki harf aralığı/kerning PyMuPDF'in kelime sınırı sezgisini bozuyor) — bu yüzden tam ifade tek parça olarak eşleşmiyor. (2) `"www.turkiyesigorta.com.tr"` ham PDF'te doğru/tam haliyle var ama üretilen EPUB metninde HİÇ yok (uçtan uca test ile doğrulandı) — muhtemelen kapak sayfasındaki kısa/tek satırlık bu blok margin veya noise filtresine takılıp siliniyor, kök neden araştırılmadı.
- [ ] (2026-08-13 audit) `process_chunk` her chunk container'ında PDF'in tamamını indiriyor — büyük kitaplarda (ör. 80MB × 40 chunk) dönüşüm başına GB'larca gereksiz Blob egress. Plan fazında PDF'i aralıklara bölmek (`doc.select()`) ya da küçük kitaplarda tek container kullanmak egress'i ~chunk-sayısı-kat azaltır.
- [ ] (2026-08-13 audit) `/analyze` ve upload-token endpoint'leri auth'suz ve rate-limit'siz — herkes 100MB'a kadar PDF yükletip Modal'da analiz çalıştırabilir (maliyet DoS'u). Genel olarak API'de hiç rate limiting yok (`@nestjs/throttler` eklenebilir).
- [ ] (2026-08-13 audit) `ConvertJobStatus.PROCESSING` hiçbir yerde set edilmiyor — lifecycle fiilen PENDING → COMPLETED/FAILED; progress raporlama için map fazından ara durum güncellemesi gerekir.
- [ ] `FloatingThemeToggle.tsx` hem `RootLayout.tsx`'te hem `Navbar.tsx`'te import ediliyor ama hiçbir yerde `<FloatingThemeToggle />` olarak render edilmiyor (ölü kod) — component'in kendi konumlama class'ı da eksik (`right-4 top-4` var ama `fixed`/`absolute` yok, render edilse bile ekranda konumlanmazdı). Kullanılacaksa gerçekten JSX'e eklenip pozisyon class'ı düzeltilmeli, kullanılmayacaksa silinmeli.
- [ ] Uygulama dili ingilizceyken satın alma penceresi türkçe açılıyor
- [ ] Yukarıdaki 400 düzelince aynı job'da `run_pipeline`'ın sonunda attığı Vercel webhook'u (`post_vercel_webhook`) `404 {"detail":"Not Found"}` dönmeye başladı — bu gövde Nest'in değil FastAPI/Starlette'in varsayılan 404 formatı, yani `publisco-secrets`'taki `VERCEL_WEBHOOK_URL` gerçek Nest API'sine değil bir FastAPI uç noktasına (büyük ihtimalle Modal worker'ın kendi `MODAL_ENDPOINT_URL`'ine) işaret ediyordu. Bu aynı zamanda Modal'ın kendi loglarındaki tekrarlayan `POST / -> 404`'ü de açıklıyor (webhook kendi kendine POST atıyor). `curl -X POST https://api.publisco.com/api/webhooks/modal-result` ile doğru URL doğrulandı (401 "Invalid webhook signature" döndü — Nest doğru route'ta canlı). Kod tarafında düzeltilecek bir şey yok; `publisco-secrets`'taki `VERCEL_WEBHOOK_URL`'in Modal dashboard'undan `https://api.publisco.com/api/webhooks/modal-result` olarak güncellenmesi gerekiyor (kullanıcıya iletildi, henüz teyit bekleniyor).

- [ ] (2026-08-18, eval framework ile bulundu — `scanned_002` golden kitabı) `detect_chapters` (`modal_worker/converter.py`) yalnızca PDF'in gömülü outline/bookmark'ından (`doc.get_toc()`) bölüm okuyor; sayfa-içi font-boyutu tabanlı bir başlık tespiti (kapak başlığı/yazarı için var olan `_pick_title_author_from_sized_lines` benzeri) yok. TOC/bookmark'ı olmayan taranmış kitaplarda (çoğu gerçek dünya taraması böyle) bu, `analyze_pdf`'in `chapters: []` dönmesi ve pipeline'ın tüm kitabı tek bir `<h1>` altında birleştirmesi anlamına geliyor — `scanned_002`'de (8 gerçek bölümlü bir roman) TOC recall %12.5 olarak gözlemlendi. Bu KASITLI bir davranış/test sözleşmesi (`tests/test_converter.py::test_detect_chapters_returns_empty_without_toc`), yanlışlıkla ortaya çıkmış bir bug değil — bu yüzden dokunulmadı. Eğer font-boyutu heuristiğiyle bir fallback eklenecekse (her sayfanın üst kısmında, gövde metninden belirgin şekilde büyük punto + kısa satır + sayfa boyunca tekrar eden bir örüntü aranabilir — bkz. bu görevde `scanned_002`'nin 8 bölüm başlığını elle bulmak için kullanılan yöntem), önce kullanıcıyla kapsam/yanlış-pozitif riski netleştirilmeli.
- [ ] Kitap adı çalışma sırasında bir kez daha teyit edilmeli: `scanned_002`'nin `input.pdf`'inde gömülü bir metin/OCR katmanı zaten var (`is_scanned=true` olsa da) ve bazı yerlerde bozuk (`"Eğer"` → `"Ecer"`) — bu, tarayıcı yazılımının kendi OCR'ı olabilir, `modal_worker` pipeline'ının ürettiği bir şey değil; ama ileride "OCR kalitesi düşük" şikayetlerinde bu PDF'e özgü bir karışıklık kaynağı olabileceği unutulmamalı.

## Çözüm Önerileri
- [ ] Convert'teki İptal butonu artık istemciyi sonucu beklemekten vazgeçiriyor ama Modal'daki `run_pipeline`'ı (bkz. `modal_worker/main.py`) sunucu tarafında durdurmuyor — pipeline kullanıcı vazgeçtikten sonra da container/OCR kaynağını tüketmeye (ve dolayısıyla Modal faturasını) etkilemeye devam ediyor. Gerçek bir sunucu-taraflı iptal için ya Modal'ın job iptal API'si (`modal.FunctionCall.cancel`) kullanılmalı ya da `ConvertJob`'a bir "cancelled" bayrağı eklenip `run_pipeline`'ın periyodik olarak (her chunk sonrası) bunu kontrol etmesi gerekiyor.
