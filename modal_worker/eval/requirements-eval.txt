# Yalnız eval framework için -- prod Modal image'ına (main.py'deki
# modal.Image) dahil EDİLMEZ, yerel geliştirme/test venv'ine kurulur:
#   .venv/bin/pip install -r eval/requirements-eval.txt
pytest==9.1.1
wordfreq==3.1.1
