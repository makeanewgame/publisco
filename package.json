{
  "name": "publisco-monorepo",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:conversion": "cd modal_worker && .venv/bin/python -m eval.run"
  },
  "devDependencies": {
    "turbo": "^2.1.3"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0"
  }
}
